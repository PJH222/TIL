using System;


public class Solution {
    public int solution(int[] num_list, int n) {
        int answer = 0;
        // bool aa = num_list.Contains(n);
        
        if (Array.IndexOf(num_list,n) != -1) {
            return 1;
        }
        
        
        return answer;
    }
}