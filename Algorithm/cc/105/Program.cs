using System;

public class Solution {
    public int solution(int num, int k) {
        int answer = -1;
        if ((num.ToString()).IndexOf(k.ToString()) != -1) return  (num.ToString()).IndexOf(k.ToString()) + 1;
        
        
        return answer;
    }
}