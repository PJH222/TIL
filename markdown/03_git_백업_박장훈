## 연결하는 방법


## git remote add origin https://github.com/PJH222/my-repo.git
- 링크로 연결하기

## git remote
- 이름이 뭔지 알려줌

## git remote origin
- 이름이 뭐고

## `git remote -v`
- 이름과, 그와 연결된 링크

## git push
- git push --set-upstream origin master <<이거 한 번 쳐주자>>
- 깃허브에 **업로드** 하는 방법임
- 중간에 아이디/비번 입력하는 부분있음

### `git push`가 진행되는 간략한 순서로는...
> 파일 수정
> git commit -am "" (add와 commit을 한번에)
> git push

혹은

> 파일 수정
> git add ""
> git commit -m ""
> git push


## git pull
- 당겨 오는 것
- 그러니까 `깃허브`에 있는 것을 당겨오는 것이라 생각해보면 됨